import { defineConfig } from 'vite';

export default defineConfig({
  base: '/michaelbeihua.github.io/', // Replace <REPOSITORY_NAME> with your actual repository name
  build: {
    outDir: 'dist'
  }
});